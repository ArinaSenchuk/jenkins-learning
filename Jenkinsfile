pipeline {
    environment {
        DOCKERFILE = "Dockerfile.build"
    }
    stages {
        stage('build') {
            steps {
                echo 'Pipeline work'
            }
        }
    }
}
